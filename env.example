# Copy to `.env.local` for local dev, or set these as environment variables in Render/Fly.

# Database
# Local dev: file:./dev.db
# Render w/ persistent disk: file:/var/data/app.db
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-me"

# Google OAuth (Google Cloud Console)
GOOGLE_CLIENT_ID="replace-me.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="replace-me"

# Optional override; if not set, the app uses NEXTAUTH_URL + /api/gmail/callback
GOOGLE_OAUTH_REDIRECT_URL="http://localhost:3000/api/gmail/callback"

# OpenAI
OPENAI_API_KEY="replace-me"
OPENAI_MODEL="gpt-4o-mini"

# Cron (protects POST /api/cron/sync)
CRON_SECRET="replace-me"



